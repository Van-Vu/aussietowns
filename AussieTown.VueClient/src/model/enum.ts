export enum ListingType { Offer, Request }

export enum RepeatedType { None, Daily, Weekly, Monthly }

export enum UserSource { Native, Facebook, Google }

export enum UserRole { User = 1, Editor = 2, Admin = 4, SuperAdmin = 8 }