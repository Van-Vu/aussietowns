export default class CheckButtonModel {
    public id: string;
    public value: string;
    public text: string;

    constructor(id, value, text) {
        this.id = id;
        this.value = value;
        this.text = text;
    }
}